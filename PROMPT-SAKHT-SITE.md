# 🏗️ پرامپت ساخت فروشگاه اینترنتی کامل (قابل استفاده با هر هوش مصنوعی)
> طرز استفاده: کل متن زیرِ خط جداکننده را کپی کن، جاهای داخل 【قوس】 را با کسب‌وکار خودت پر کن و به هوش مصنوعی بده.
> مثال پرکردن: 【نوع کالا】= لوازم الکتریکی و صنعتی · 【نام فروشگاه】= برق‌سان · 【شهر】= تهران
> نمونهٔ مرجع این پرامپت: **«الکتریکی و قطعات الکترونیک یاسایی تهران»** — تاریخ رسمی نسخه: **شهریور ۱۴۰۵ هجری شمسی (1405 SH)**.

---

برای من یک فروشگاه اینترنتی کامل، موبایل‌اول، سریع و بدون باگ بساز با این مشخصات دقیق.
کد را در هر پاسخ به‌صورت فایل‌های کامل بده و هر بخش را با کامنت فارسی توضیح بده.

## الزامات فنی سخت
1. بک‌اند: Node.js خالص (ESM) بدون هیچ وابستگی npm. داده‌ها در یک فایل JSON با:
   نوشتن اتمیک (فایل موقت + rename)، صف تراکنش سریال (db.tx)، پشتیبان خودکار دوره‌ای،
   و بازیابی خودکار از تازه‌ترین پشتیبان اگر فایل اصلی خراب بود.
2. فرانت‌اند: SPA بدون فریم‌ورک؛ ماژول‌های ES؛ مسیریاب هش‌محور با بارگذاری تنبل نماها؛
   قالب‌سازی امن با escape خودکار؛ بدون CDN و بدون فونت/کتابخانهٔ خارجی (قابل اجرا در ایران).
3. PWA: manifest + سرویس‌ورکر با کش پوسته و کش دادهٔ خواندنی‌ها و صفحهٔ آفلاین؛
   نسخهٔ سرویس‌ورکر با هر انتشار عوض شود تا کش کهنه نماند.
4. ساختار پوشه: server/ (main + lib/ + api-*.mjs به تفکیک حوزه)، public/ (js/views, js/lib, css, assets)،
   tests/ (چهار سوئیت تست در پرامپت اولیه؛ امروز در عمل ۲۳ سوئیت — فهرست کامل در `tests/verify.mjs`)،
   tools/ (جاروب باگ با مرورگر واقعی، ساخت نسخهٔ تک‌فایلی آفلاین).

## زبان و ظاهر
5. دوزبانه: 【زبان اصلی، راست‌به‌چپ؟】 پیش‌فرض + زبان دوم با یک کلیک؛ همهٔ متن‌ها از یک فایل i18n کلیددار.
6. دو حالت رنگی تاریک/روشن (تاریک پیش‌فرض)؛ رنگ برند 【رنگ برند، مثل #f59e0b】؛ فونت بومی بدون منبع خارجی.
7. یک کلید «پوستهٔ ظاهری» در پنل مدیر با دو مقدار (تازه/کلاسیک) که استایل‌های تازه فقط زیر
   `html[data-variant="fresh"]` باشند تا برگشت به پوستهٔ قبلی بدون ریسک ممکن باشد.
8. ولکام‌اسکرین برندینگ‌شده (لوگو + نام + اسپینر) که تا آماده‌شدن برنامه بماند و نرم محو شود؛
   حداقل ۷۰۰ms نمایش؛ با prefers-reduced-motion بدون انیمیشن.

## قابلیت‌های فروشگاهی (همه لازم‌اند)
9. کالا با دسته/برند/ویژگی‌ها/گالری تصویر/ویدیو/بارکد/موجودی/تخفیف/نشان‌ها؛ جست‌وجوی fuzzy فارسی
   (نرمال‌سازی ارقام و حروف، فاصلهٔ لوِنشتاین، «منظورتان این بود؟») + جست‌وجوی تصویری.
10. سبد مهمان (کوکی) که پس از ورود به سبد حساب می‌چسبد؛ نوار پیشرفت «ارسال رایگان از X تومان»؛
    نوار خرید چسبان در موبایل؛ کالاهای مشابه؛ «اخیراً دیده‌اید»؛ لیست علاقه‌مندی؛ مقایسه.
11. سفارش با چند روش ارسال (پیک محلی/پست/تحویل حضوری) و بیمارهٔ اختیاری؛ لغو خودکار سفارش پرداخت‌نشده
    پس از N ساعت با بازگشت موجودی؛timeline وضعیت سفارش؛ فاکتور؛ کیف پول و امتیاز و membership پولی.
12. سه روش ورود: نام‌کاربری+رمز، کد یک‌بارمصرف پیامکی، کد ایمیلی + ورود دومرحله‌ای TOTP اختیاری؛
    قفل موقت پس از تلاش ناموفق + کپچای خودمیزبان (بدون سرویس خارجی).
13. صفحهٔ اطلاعات: درباره/راهنما/گارانتی/قوانین/حریم‌خصوصی/بیمه/تماس با فرم و نقشهٔ انتخاب نقطه (SVG) + کارت ربات پشتیبانی تلگرام (لینک از تنظیمات، پنهان‌شونده اگر خالی).
14. ویجت چت پشتیبانی زنده (رشتهٔ مشترک کاربر-مدیر)، تیکت، بازخورد، قرعه‌کشـی، کوپن، تبلیغات بنری،
    اعلان‌ها (SSE + زنگوله)، خبرنامه، «اطلاع بده وقتی موجود شد».
14b. ربات پشتیبانی تلگرام (وب‌هوک با کلید مخفی در اولویت + فال‌بک long-poll تک‌حلقه‌ای): /start عضویت+خوش‌آمد، /status کدسفارش با راهنمای فارسی
    برای کد خالی/اشتباه، /products جستجو با نمونه‌کالا، /contact، راهنما برای دستور ناشناخته،
    پیام متنی → صندوق پنل + پاسخ خودکار؛ پخش همگانی به مشترک‌ها؛ نمایش «وضعیت اتصال ربات» در پنل.
14c. ذره‌بین زوم تصویر کالا در هاور دسکتاپ (~۲٫۶x، لمس‌ها مستثنا)؛ نگهبان تازگی نسخه: مقایسهٔ build کلاینت/سرور
    و تاست «نسخهٔ جدید» + ثبت SW با updateViaCache:'none' و به‌روزرسانی دوره‌ای (درمان کش‌های کهنگرفته).

## پنل مدیریت (با ماتریس مجوز ~۲۰ تایی و لاگ ممیزی فراگیر)
15. داشبورد شاخص‌ها + نمودار؛ مدیریت کامل کالا/دسته/برند/سفارش/کاربر/نظر/تیکت/کوپن/تبلیغ/قرعه‌کشـی؛
    بازدیدکنندگان با تجزیهٔ User-Agent؛ پخش همگانی چندکاناله (سایت/تلگرام/ایمیل/پیامک).
15b. بستهٔ پست اینستاگرام برای هر کالا: تصویر+دانلود، کپشن آمادهٔ قابل‌ویرایش (قیمت/تخفیف/موجودی/لینک/تلفن)،
    هشتگ‌های خودکار، دکمه‌های کپی متن/هشتگ/هر دو.
16. کنسول سامانه: خواب/بیداری فروشگاه با زمان‌بندی، ریستارت، ریست بخش‌ها، پشتیبان‌گیری/بازیابی.
17. کارت زندهٔ «پدافند»: درخواست همزمان، rps، طول صف، مسدودهای خودکار، پرمراجع‌ترین IPها،
    و فهرست خطاهای زندهٔ کلاینت (گزارش خودکار مرورگرها به POST /api/client-error).

## امنیت و پایداری (خط قرمز)
- هرگز ورودی کاربر را خام داخل پیام قالب‌دار (HTML تلگرام، href، innerHTML) نگذار: escape/سانیتایز اجباری.
- لینک‌های تنظیم‌شدنی توسط ادمین فقط با اسکیم http/https (سرور + کلاینت هر دو).
- endpointهای عمومی حداقل اطلاعات ممکن را بدهند (نام سرویس/مسیر داخلی/توکن هرگز).
18. هش رمز scrypt با salt و مقایسهٔ زمان‌ثابت؛ نشست HttpOnly SameSite؛ CSRF دوطرفه؛ هدرهای
    CSP/HSTS/nosniff/DENY؛ اعتبارسنجی همهٔ ورودی‌ها در سرور؛ آپلود فقط تصویر با بررسی واقعی.
19. ضد-DDoS: شمارش درخواست همزمان و rps (فقط درخواست سنگین شمرده شود)؛ وقتی از آستانه گذشت
    بازدیدکنان ناشناس به «اتاق انتظار» بروند: صفحهٔ نوبت دوزبانه با نظرسنجی دوره‌ای و پذیرش دونه‌دونه
    با کوکی گذرنامهٔ امضاشدهٔ HMAC؛ کاربران واردشده معاف؛ مسیرهای حیاتی (health/status/queue) معاف؛
    نظرسنجی صف هرگز در سنجهٔ بار شمرده نشود (بن‌بست صف).
20. مسدودسازی خودکار سیلاب (آستانهٔ درخواست/دقیقه per-IP) با انقضا و ثبت در لیست بن‌ها؛
    بن‌های زمانی در همهٔ دروازه‌ها رعایت و خودکار پاک‌سازی شوند.
21. خودترمیمی: بازسازی مجموعه‌های مفقود دیتابیس در بوت و واچ‌داگ؛ error boundary برای هر بخش صفحه؛
    خطای ناگیره سرور سایت را نخواباند؛ گزارش خودکار خطای کلاینت به پنل مدیر.

## تجربهٔ کاربری ریز
22. دکمهٔ رفرش در هدر؛ تولتیپ (title) روی همهٔ دکمه‌ها/آیکون‌ها (کپی خودکار از aria-label)؛
    اسکرول‌بار همرنگ تم؛ اعداد فارسی؛ فرمت پول و تلفن بومی؛ حالت eco و کاهش انیمیشن.
23. مارکی/نوار متحرک برندها باید در RTL و LTR هر دو بی‌نهایت و بدون درز حلقه بزند
    (جهت حرکت از computed direction خوانده شود، نه ثابت).

## تحویل و تست (اجباری)
24. چهار سوئیت تست بدون وابستگی در نسخهٔ اول (API، endpointها، دود HTTP، پارس کلاینت — امروز ۲۳ سوئیت) + جاروب باگ و جاروب تعامل
    با مرورگر واقعی (Playwright)؛ هر باگ دیده‌شده در مرورگر = باگ، نه فقط تست سبز.
25. نسخهٔ تک‌فایلی آفلاین کل سایت با یک اسکریپت بساز؛ راهنمای فارسی اجرا + سند «چگونه خودم دیپلوی کنم»
    (Render رایگان: build=echo ready، start=node server/main.mjs، region نزدیک ایران) بنویس.
26. در پایان هر راند: ZIP کامل پروژه + گزارش فارسی تغییرات + فهرست کارهایی که فقط خودم باید انجام دهم.

## سبک کدنویسی
27. کد مختصر، بدون تکرار، با کامنت فارسی بالای هر بخش و هر تابع غیربدیهی؛ نقشهٔ کد در سرصفحهٔ
    فایل‌های ورودی؛ هیچ رشتهٔ رابط کاربری hardcoded بیرون i18n؛ هیچ کلید API داخل کد (همه از تنظیمات مدیر).

## افزوده‌های راند ۲۹ (ys-v6)
28. کالای بدون‌قیمت (خدمات/سفارشی): به‌جای «۰ تومان» برچسب «استعلام قیمت»، دکمه‌های افزودن/خرید حذف و
    CTA تماس تلفنی + مراجعهٔ حضوری نمایش داده شود؛ سرور هم افزودن چنین کالایی به سبد را رد کند (422).
29. کارت «قطعه‌ای پیدا نکردی؟» در خانه با دکمهٔ تماس/پیام/اینستاگرام؛ شمارهٔ ثابت با گروه‌بندی خوانا
    (۰۲۱-۷۷۹۰-۶۶۶) در fmtTel قالب‌بندی شود.
30. شبکه‌های اجتماعی تنظیمات فروشگاه فقط لینک‌های واقعی خود پروژه باشد (نشتی لینک پروژهٔ دیگر ممنوع).

### راند ۳۱
- اضافه شدن امکان ثبت «گزارش قیمت پایین‌تر» در صفحه محصول (همراه با بخش مدیریت برای مشاهده گزارش‌ها).
- اضافه شدن فیلد «لینک منبع قیمت» (آزمایشی و برای توسعه‌های آینده و اطلاع‌رسانی) در فرم کالا در ادمین.
- اضافه شدن آیتم «تضمین بهترین قیمت» به صفحه اصلی.
- ارائه مشاوره‌های قیمتی (فروش سایت و پشتیبانی) و توضیحات درگاه پرداخت، ای‌نماد و پیامک.
- تست سلامت و باگ‌گیری مجدد روی هر دو پروژه انجام شد.
- پیاده‌سازی ربات پاسخگوی خودکار در گفتگوی آنلاین (پشتیبانی سایت) همراه با امکان ارسال ایمیل و پیامک (نوتیفیکیشن) برای تمامی پیام‌های پشتیبانی.
- اضافه شدن سیستم مانیتورینگ «جستجوهای ناموفق». در صورتی که کاربری کالایی را بیشتر از ۵ بار جستجو کند اما سایت آن را نداشته باشد، به صورت خودکار به ادمین هشدار ارسال می‌شود. همچنین گزارش کامل این کلمات در پنل مدیریت (بخش آمار و بینش) قابل مشاهده است.
- رفع مشکل بای‌پس قوانین و مقررات: اگر کاربری هنگام نمایش مودال پذیرش قوانین، روی خواندن قوانین کلیک می‌کرد، به صفحه قوانین می‌رفت اما مودال دیگر به او نمایش داده نمی‌شد. هم‌اکنون این مشکل رفع شده و تا زمانی که واقعاً قوانین را قبول نکند، سایت او را ملزم به تایید قوانین می‌داند. علاوه بر این یک دکمه‌ی میانبر تایید قوانین در همان صفحه‌ی متنی قوانین قرار داده شد تا راحتی کاربر بیشتر شود.
- بخش خرید مهمان به صورت کامل غیرفعال شد؛ کاربران دیگر نمی‌توانند بدون حساب کاربری سفارشی ثبت کنند و در سبد خرید حتماً به صفحه‌ی لاگین هدایت می‌شوند. 
- باگ گم‌شدن سبد خرید رفع شد: حالا اگر کاربر در حالت مهمان کالاهایی را به سبد خرید اضافه کند و سپس لاگین کند یا ثبت‌نام انجام دهد، آن کالاها به طور خودکار به حساب کاربری او منتقل (Merge) می‌شوند تا کاربر مجبور نباشد دوباره سبدش را بچیند.
- چندین آسیب‌پذیری سرریز حافظه (OOM Memory Leaks) ناشی از جستجوهای بی‌نهایت و ذخیره گزارش‌ها در سرور شناسایی و برای آن‌ها سقف مجاز حافظه تعریف شد.
- باگ UI مربوط به اسکرول‌بار مرورگر در اندروید (خط آبی در لبه‌های کناری) برطرف شد. اسکرول‌بار سفارشی برای دستگاه‌های لمسی غیرفعال شد تا از تداخل رابط کاربری جلوگیری شود.

## محدودیت‌ها و معماری امنیتی پدافندی (بسیار مهم برای هوش‌های مصنوعی آینده)
14. **Safe Cancel (لغو امن و متمرکز):** هر لغو سفارشی (توسط ادمین، اتوماتیک پس از ۷۲ ساعت یا ناموفق در درگاه) باید از طریق متد `safeCancelOrder` در `api-shop.mjs` عبور کند تا موجودی کالا، برگشت وجه به کیف پول، بازگشت کوپن تخفیف و امتیازات وفاداری بدون Race Condition بازیابی شوند.
15. **محدودیت‌های رشد آرایه‌ها (Array Bounds Enforcement):** برای جلوگیری از پر شدن حافظه (Memory Bloat DoS):
   - **سبد خرید (Cart):** حداکثر ۵۰ آیتم. سبدهای مهمانِ رهاشده پس از ۳ روز و عضوها پس از ۳۰ روز توسط Watchdog پاک می‌شوند.
   - **کیف پول (Wallet):** تاریخچه تراکنش‌ها همواره روی حداکثر ۲۰۰ آیتمِ آخر (slice / length truncate) قفل می‌شود.
   - **تیکت (Tickets):** حداکثر ۱۰۰ پیام برای هر تیکت (جلوگیری از ریپلای‌های اسپم).
   - **نشست‌ها (Sessions):** حداکثر ۱۰ نشست فعال برای هر کاربر. فراتر از آن نشست‌های قدیمی‌تر منقضی می‌شوند.
   - **خبرم کن (Price Alerts):** حداکثر ۵۰ اعلان فعال برای هر کالا/کاربر.
16. **آپلودها (Upload Limits):** حداکثر ۳۰ آپلود برای هر کاربر در هر ۲۴ ساعت با فایل‌سایز حداکثر ۴ مگابایت محدود شده است تا فضای هاست توسط ربات‌ها اشغال نشود.
17. **Watchdog سیستم (Cron-like):** در پس‌زمینه‌ی `main.mjs` یک تایمر همواره دیتابیسِ JSON را چک می‌کند، نشست‌های باطل‌شده، کوپن‌های باطل‌شده، ربات‌های اسپم (Guest Carts) و بن‌های موقت را هرس (Prune) می‌کند تا سرور هرگز کُند نشود.

## آخرین تغییرات و باگ‌گیری‌های نسخه جدید (شهریور ۱۴۰۵)
- **بازنویسی کامل پرینت فاکتور (Print Invoice):** حل مشکل صفحه‌ی سفید یا خالی افتادن فاکتور در چاپ با جدا کردن `inv-paper` از ساختار DOM هنگام فشردن دکمه چاپ و `position: static` کردن لایه‌ها. 
- **جلوگیری از DoS در آرایه‌های دیتابیس:** اصلاح طول آرایه‌هایی نظیر `supportMessages`, `tickets`, `reviews`, `carts`, `visitors`, `audit` و `priceAlerts` در فایل‌های سرور (`api-shop.mjs`، `api-admin.mjs`، `lib/db.mjs`، `lib/helpers.mjs`) به حداکثر مقادیر ایمن (۵۰۰ تا ۱۰۰۰ رکورد) جهت پیشگیری از پر شدن حافظه (Memory Exhaustion).
- **اصلاح پاپ‌آپ آپدیت (Update Toast):** تطبیق متغیر `BUILD` در کلاینت (`state.mjs`) و سرور (`lib/util.mjs`) تا پیام "نسخه جدید سایت آماده است" فقط زمان آپدیت واقعی نمایش داده شود.
- **تکمیل فیلدهای فاکتورساز:** اضافه شدن فاصله‌ی مناسب برای مشخصات خریدار، افزودن کد ملی/اقتصادی فروشنده از تنظیمات و ایجاد حاشیه‌ی مناسب برای دکمه‌ی "افزودن به لیست".
- **جداسازی کامل پروژه‌ها:** اطمینان از تفکیک کامل کدهای پروژه ۱ (Yassaei Electronics) و پروژه ۲.



## آخرین تغییرات و وضعیت پایداری نهایی (نسخه تضمین‌شده - ۲۴ شهریور ۱۴۰۵)
این نسخه یک نسخه کاملاً پایدار، باگ‌گیری شده و نهایی است که تمامی امکانات زیر در آن تست و تأیید شده‌اند:
۱. **فاکتورساز لحظه‌ای و چاپ (Print Invoice):** کدها جوری بازنویسی شدند که هنگام فشردن دکمه چاپ، فاکتور از لایه‌های تودرتوی ادمین جدا شده و مستقلاً چاپ شود تا مشکل سفیدی صفحات و تنظیم نبودن ابعاد کاغذ به طور کامل برطرف گردد.
۲. **امنیت حافظه و مقابله با Database Bloat:** تمام آرایه‌های در حال رشد سیستم (نظیر `supportMessages`, `carts`, `visitors`, `audit`, `reviews` و لیست تراکنش‌های کیف پول) به طور دقیق محدود شده‌اند (سقف ۵۰ تا ۵۰۰ رکورد بسته به کاربرد) تا سرور هرگز دچار کمبود حافظه (DoS) نشود.
۳. **رفع پاپ‌آپ‌های مزاحم بروزرسانی:** یکپارچه‌سازی متغیر `BUILD` بین فرانت‌اند (`state.mjs`) و بک‌اند (`util.mjs`) به منظور جلوگیری از نمایش خطای "نسخه جدید سایت آماده است" در مواقعی که آپدیتی وجود ندارد.
۴. **ایزوله‌سازی پروژه‌ها:** پروژه سیب سبز (موبایل) و پروژه یاسایی (الکترونیک) به صورت ۱۰۰٪ مجزا بدون هیچگونه تداخل کدی مدیریت و مستندسازی شده‌اند.
۵. **فایل‌های آفلاین:** نسخه‌های `offline.html` با کدهای جدید همگام‌سازی شده و از طریق `standalone-build.mjs` بازسازی کامل شده‌اند.

## افزوده‌های راند ۳۲ — الزامات نسخهٔ رسمی `v1.0.0` (۲۹ شهریور ۱۴۰۵)
این هفت قابلیت، ستون‌های نسخهٔ رسمی‌اند و بازتولید سایت بدون آن‌ها کامل محسوب نمی‌شود:

31. **سقف خرید هوشمند غلتان (Rolling Purchase Ceiling):** مجموع مبالغ سفارش‌های **موفق/پرداخت‌شدهٔ**
    هر کاربر (یا سبد مهمان با guestId) در بازهٔ غلتان N ساعت (پیش‌فرض ۴۸) از سقف X تومان (پیش‌فرض ۱۰۰ میلیون)
    کم می‌شود؛ `remaining = max(0, X − pastSpent)`؛ سفارش پرداخت‌نشده/لغوشده/مرجوع **هرگز شمرده نمی‌شود** و
    هیچ محرومیت دائمی وجود ندارد (با اسلاید بازه سهمیه برمی‌گردد). اجرای قانون داخل همان تراکنش ثبت سفارش و
    **قبل از کسر موجودی/کیف پول**. سه کلید (فعال/مبلغ/بازه) از پنل قابل تنظیم + وضعیت زندهٔ سهمیه به checkout
    ارسال شود تا کاربر پیام محترمانهٔ فارسی با مبلغ باقی‌مانده ببیند. مکمل: سقف روزانهٔ ضدسیلاب بر پایهٔ
    hash-deviceToken و hash کارت/شبای پرداخت‌کننده.
32. **واتساپ فروش:** دکمهٔ «مشاورهٔ فنی در واتساپ» در صفحهٔ کالا و دکمهٔ «ارسال فیش پرداخت در واتساپ» در
    صفحهٔ موفقیت سفارش؛ ساخت لینک `wa.me` با ترتیب اولویت (شمارهٔ اختصاصی ابزار ← واتساپ فروشگاه ← شبکه‌های
    اجتماعی ← موبایل دوم ← تلفن) و نرمال‌سازی خودکار هر فرمت (`0912…/+98…/0098…`); متن پیام آمادهٔ فارسی.
    همه با کلید فعال/غیرفعال از پنل؛ پیش‌فرض روشن (`!== false`) تا بذرهای قدیمی رگرسیون نگیرند.
33. **پیش‌فاکتور رسمی (Proforma):** دکمهٔ «دریافت پیش‌فاکتور رسمی» در سبد؛ برگهٔ چاپی مستقل (لایهٔ جدا از SPA)
    با سربرگ/اطلاعات فروشگاه، جدول کالا، جمع‌ها، تاریخ شمسی و کد یکتای `PF-YYYYMMDD-jalali-XXXXX`
    (تبدیل میلادی→شمسی الگوریتمی بدون Intl). در همان صفحه: کارت «کارت‌به‌کارت» (بانک/کارت/شبا/صاحب‌حساب از
    پنل، فرمت‌دهی گروهی ۴تایی ارقام محلی) و نوار «تا ارسال رایگان».
34. **بخش استخدام (Careers):** صفحهٔ عمومی `#/careers` (فرصت‌های باز + فرم درخواست: نام/موبایل الزامی با
    الگوی `09\d{9}`، سن ۱۶–۸۰، تحصیلات/سابقه، رزومهٔ اختیاری فقط از مسیرهای `/uploads/`، ضدتکراری
    موبایل+شغل، rate-limit ۴/روز مهمان و ۱۰/روز کاربر). پنل ادمین CRUD فرصت‌ها + جدول درخواست‌ها با
    وضعیت/یادداشت؛ اعلان سراسری به مدیر و کارمندان هنگام درخواست جدید؛ کلید خاموش‌کردن کل بخش
    (`features.careers`) + کلید نمایش در منو؛ مسیر عمومی ۴۰۴ محترمانه وقتی خاموش است.
35. **چت داخلی تیم:** اتاق گفت‌وگوی `owner/staff` (کاربر عادی ۴۰۳؛ کلید `features.adminChat`):
    `GET/POST /api/admin/chat` + استریم SSE برای تحویل زنده؛ پیام ≤۲۰۰۰ نویسه، پیوست ≤۴ مورد فقط
    `/uploads/`، mention با `@username`، ارجاع ماشه‌ای `#order/#product/#ticket/#user` که پیل
    لینک‌شدهٔ کلیکی رندر می‌شود؛ تاریخچه سقف‌دار (۲۰۰ اخیر / ۱۰۰۰ کل) و ممیزی هر پیام.
36. **مدیر چیدمان (Layout Manager) بدون کدنویسی:** صفحهٔ `#/admin/layout` با لیست Drag&Drop تمام بلوک‌های
    صفحهٔ اصلی (هیرو، استریپ، دسته‌ها، برندها، ویژه‌ها، شگفت‌انگیز، قیمت‌قدیم، پرفروش‌ها، دست‌دوم‌ها،
    تازه‌ها، برندها، findPart، چرا ما، آمار، پلاس، دانلود اپ، بازدید اخیر، مینی‌مپ فوتر، خرید دست‌دوم):
    تغییر ترتیب + مخفی/ظاهر (`settings.layout.homeOrder` و `homeHidden`) + تب «منوهای پنل» برای
    نمایش/نام/آیکون/ترتیب ناوبری ادمین، منوی کاربری و نوار موبایل. ذخیره با sanitizeSection اختصاصی.
37. **ابزار نجات اضطراری مالک:** `node tools/unban-owner.mjs <phone_or_username>` — پیداکردن حساب با
    موبایل/نام‌کاربری (ارقام فارسی نرمال)، ساخت مجموعهٔ هویت‌ها (phone/username/email/lastIp/visitorTokens +
    IPهای نشست‌ها + IP بازدیدکنندگان)، حذف فقط بن‌های آن مجموعه، بازگرداندن `role=owner/status=active`،
    نوشتن ممیزی `owner.emergency_unban`. **قانون سرخ: هیچ رکورد داده/سفارش/موجودی حذف یا تغییر نمی‌کند.**
38. **چرخهٔ انتشار رسمی (Release):** تگ BUILD همگام در ۵ نقطه (`npm run bump`)؛ بازسازی باندل esbuild و
    نسخهٔ تک‌فایلی آفلاین با `node tools/standalone-build.mjs` (بذر پاک‌سازی‌شده: بدون هش رمز، بدون توکن
    بات، مجموعه‌های شخصی خالی — جاروب `secret-scan` باید سبز بماند)؛ `npm run build && npm run verify`
    ۱۰۰٪ سبز؛ سپس `gh release create vX.Y.Z yassaei-offline.html --title … --notes …` روی `main` و
    به‌روزرسانی متادیتای مخزن (`gh repo edit --description … --add-topic …`). هر Release = یک فایل آفلاین
    سالم و قابل آفلاین‌اجرا ضمیمه شود.

## افزوده‌های راند ۳۳ — محافظت از کد و یکدست‌سازی تاریخ شمسی (شهریور ۱۴۰۵ هجری شمسی · 1405 SH)

39. **کلید امنیتی `antiInspectEnabled` (پیش‌فرض `true`) در `settings.security`:** در `defaults.mjs`
    تعریف شود، در `sanitizeSection('security')` با `V.bool(v, true)` پاک‌سازی شود و در `/api/bootstrap`
    **فقط همین یک کلید** به‌صورت `security: { antiInspectEnabled: s.security?.antiInspectEnabled !== false }`
    عمومی شود (پارامترهای صف/محدودسازی نرخ هرگز به مرورگر نروند). صفحهٔ پنل: تب
    `#/admin/settings/security` در `views/admin/settings.mjs` (به‌علاوهٔ کلیدهای صف) + دکمهٔ میان‌بر در
    کارت امنیتی داشبورد.
40. **محافظت سمت کاربر (ماژول `public/js/lib/anti-inspect.mjs`، راه‌اندازی از `main.mjs`):**
    - بستن منوی کلیک راست روی عناصر عادی با `preventDefault`، **بدون** محدودکردن
      `input/textarea/select/[contenteditable]` (چسباندن، غلط‌یاب و انتخاب متن باید آزاد بماند).
    - مسدودسازی میان‌برهای `F12`، `Ctrl+Shift+I`، `Ctrl+Shift+J`، `Ctrl+Shift+C`، `Ctrl+U`
      و در مک `Cmd+Opt+I/J/C` — بدون بستن هیچ کلید سالم دیگری (`Ctrl+C/V/S`، تایپ، ناوبری).
    - هشدار امنیتی قرمز و توپر در کنسول با متن ثابت:
      «هشدار امنیتی فروشگاه یاسایی: هرگونه تلاش برای دستکاری، بازرسی یا کپی‌برداری غیرمجاز ثبت و پیگرد قانونی دارد.»
    - `draggable="false"` روی همهٔ تصاویر (شامل تصاویر تازه‌رندرشده با `MutationObserver`)؛
      عناصر با `draggable="true"` (پنل چیدمان) باید آزاد بمانند.
    - واکنش زنده به تنظیمات: با ذخیرهٔ کلید در پنل، همان لحظه (رویداد `settings` در `state.mjs`)
      محافظت روشن/خاموش شود.
41. **تست رگرسیون `tests/anti-inspect.mjs`** (در `npm run verify`): رفتار DOM واقعی با jsdom
    (کلیک راست، میان‌برهای DevTools، **آزادی کامل تایپ/کپی**، کشیدن تصاویر، حالت خاموش) + کلید سرور
    (پیش‌فرض، دسترسی مهمان ۴۰۱/۴۰۳، ذخیره/بازگردانی و باقی‌ماندن تنظیمات صف). `render-admin.mjs` هم
    تب `settings/security` را رندر می‌کند.
42. **قاعدهٔ تاریخ:** هر اشارهٔ نسخه/انتشار در مستندات باید **شهریور ۱۴۰۵ هجری شمسی (1405 SH)** باشد؛
    تاریخ‌های اشتباه تایپی (مثل ۱۳۰۵) و نام‌های پروژهٔ دیگر (مثل «تهران موبایل»/`bander-*`) مطلقاً
    نباید در اسناد بمانند. باندل، `sw.js` (پیش‌کش) و فایل تک‌فایلی `yassaei-offline.html` پس از هر
    تغییر فرانت‌اند باید بازسازی شوند: `npm run build` + `node tools/gen-sw-precache.mjs` +
    `node tools/standalone-build.mjs`.
