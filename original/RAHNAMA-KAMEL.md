# 🍏 راهنمای کامل سایت یاسایی — هر چیزی که باید بدانی
> **الکتریکی و قطعات الکترونیک یاسایی تهران** (نارمک، هفت‌حوض)
> آخرین به‌روزرسانی: **۲۹ شهریور ۱۴۰۵ هجری شمسی** (۲۰۲۶-۰۹-۲۰) · نسخهٔ زنده: ys-v138 · **انتشار رسمی: `v1.0.0` (GitHub Release)** · لینک: https://yassaei-electronics.onrender.com
>
> 📅 تاریخ رسمی این نسخه: **شهریور ۱۴۰۵ هجری شمسی (1405 SH)** — هر تاریخ دیگری در نسخه‌های قدیمی، اشتباه تایپی است.

این سند طوری نوشته شده که **اگر این چت برای همیشه از بین رفت**، بتوانی به‌تنهایی سایت را نگه داری،
اجرا کنی، توسعه بدهی و حتی یک سایت مشابه برای کسب‌وکار دیگری بسازی.

---

## ۱) سایت چیست و از چه اجزایی تشکیل شده؟

یک فروشگاه اینترنتی کامل (ای‌کامرس) با این ویژگی‌ها:
- **بدون هیچ وابستگی خارجی**: نه npm، نه دیتابیس خارجی، نه CDN. فقط `node server/main.mjs`.
- **بک‌اند**: Node.js خام (ESM) در پوشهٔ `server/` — داده‌ها در `data/db.json` (JSON با نوشتن اتمیک و تراکنش سریال).
- **فرانت‌اند**: SPA بدون فریم‌ورک در `public/` — ماژول‌های ES، مسیریاب هش‌محور، قالب امن خودساخته.
- **PWA/Aفلاین**: سرویس‌ورکر `public/sw.js` پوسته و داده‌ها را کش می‌کند؛ سایت بدون اینترنت هم باز می‌شود.
- **نسخهٔ تک‌فایلی آفلاین**: `yassaei-offline.html` — کل سایت (فروشگاه + پنل) در یک فایل HTML؛ با `node tools/standalone-build.mjs` بازسازی می‌شود و در Release رسمی `v1.0.0` هم ضمیمه است.
- **ابزارهای فروش**: مشاوره و فیش پرداخت در **واتساپ**، **پیش‌فاکتور رسمی (Proforma)** با کد شمسی، **سقف خرید هوشمند غلتان**، کارت‌به‌کارت، نوار ارسال رایگان — بخش ۵٫۹ را ببین.
- **تعامل تیم**: **چت داخلی تیم** (مدیر + کارمندان) و **سیستم استخدام/فرصت‌های شغلی** با پنل کامل.
- **مدیریت چیدمان**: پنل ← «چیدمان سایت» — تغییر ترتیب و مخفی‌کردن هر بخش صفحهٔ اصلی با کشیدن‌ورهاکردن + ویرایشگر منوها.
- **ابزار نجات اضطراری**: `node tools/unban-owner.mjs <phone_or_username>` برای بازکردن بن خودِ مالک بدون حذف هیچ داده‌ای.
- **دوزبانه**: فارسی (پیش‌فرض، راست‌به‌چپ) + انگلیسی با یک کلیک.
- **دو پوستهٔ رنگی**: تاریک (پیش‌فرض) + روشن؛ و دو «پوستهٔ ظاهری»: تازه (پیش‌فرض) / کلاسیک (قابل برگشت از پنل مدیر).

### نقشهٔ پوشه‌ها
| مسیر | محتوا |
|---|---|
| `server/main.mjs` | سرور اصلی؛ ترتیب مهم: بن IP → سنجهٔ بار → محدودساز نرخ → CSRF → نشست → اتاق انتظار → مسیریاب → استاتیک → SPA |
| `server/lib/db.mjs` | ذخیره‌ساز JSON + تراکنش‌ها + **بازیابی خودکار از پشتیبان هنگام خرابی** |
| `server/lib/auth.mjs` | رمز scrypt، نشست HttpOnly، CSRF، ورود دومرحله‌ای TOTP |
| `server/lib/queue.mjs` | اتاق انتظار ضد-DDoS (صف نوبت، گذرنامهٔ HMAC، سنجه‌های بار) |
| `server/lib/backup.mjs` | اسنپ‌شات خودکار هر چند ساعت در `data/backups/` |
| `server/lib/telegram.mjs` / `mail.mjs` / `sms.mjs` | کانال‌های اطلاع‌رسانی (نیازمند توکن/کلید خودت) |
| `server/api-*.mjs` | مسیرهای API: کاتالوگ / احراز هویت / فروش / مدیر |
| `public/js/views/*` | هر صفحه یک ماژول با `render()/mount()` |
| `public/js/views/admin/*` | پنل مدیریت (داشبورد، کالاها، سفارش‌ها، کاربران، تنظیمات،…) |
| `public/sw.js` | سرویس‌ورکر (نسخهٔ فعلی ys-v138) |
| `public/js/lib/sales.mjs` | واتساپ، پیش‌فاکتور، کارت‌به‌کارت، ارسال رایگان، تاریخ شمسی |
| `tools/` | اسکریپت‌های تست، جاروب باگ، `standalone-build.mjs` (نسخهٔ آفلاین)، `unban-owner.mjs` (نجات مالک)، keepalive، bump-build |
| `tests/` | ۲۳ سوئیت تست: api، endpoints، http-smoke، sales-tools، careers، admin-chat، anti-inspect، order-ceiling، races، … (`npm run verify`) |
| `yassaei-offline.html` | **نسخهٔ تک‌فایلی آفلاین کل سایت** (برای نمایش بدون سرور؛ ضمیمهٔ Release رسمی) |

---

## ۲) حساب‌ها و رمزها (فوری عوضشان کن اگر لو رفته‌اند)

| چه | چه چیزی |
|---|---|
| مدیر سایت | کاربر `admin` رمز `Yassaei@1404` (اولین ورود باید عوض شود) |
| کارمند | `staff` / `Staff@1404` |
| کاربران نمونه | `maryam` `reza` `sina` / `Demo@1404` |
| Render (هاست ابری) | کلید API در دست این چت بود → **از داشبورد Render باطلش کن و جدید بساز** |
| GitHub | توکن push در این چت بود → **باطل کن** (Settings → Developer settings → Tokens) |
| UptimeRobot | مانیتور ساختی؛ آدرسش را به لینک جدید عوض کن: `https://yassaei-electronics.onrender.com/api/system/status` |

---

## ۳) اجرا روی کامپیوتر خودت (لوکال)

```bash
# ۱) نصب Node.js نسخهٔ ۱۸ به بالا از nodejs.org
# ۲) باز کردن پوشهٔ سایت و اجرا:
node server/main.mjs
# ۳) مرورگر: http://localhost:3000
```
همین. هیچ `npm install` لازم نیست. برای تست‌ها:
```bash
node tests/api.mjs && node tests/endpoints.mjs && node tests/http-smoke.mjs
BM_QUEUE=off node server/main.mjs   # حالت تست: اتاق انتظار خاموش تا سوئیت‌ها صف نشوند
BM_TG=off node server/main.mjs       # بات تلگرام خاموش (وقتی نسخهٔ زنده فعال است، لوکال را بدون بات اجرا کن تا کاربر پاسخ دوبله نگیرد)
```

---

## ۴) اجرای واقعی روی اینترنت (کاری که خودم انجام دادم — تو هم می‌توانی)

### الف) هاست رایگان Render (همین الان فعال است)
۱. در render.com حساب بساز → New → Web Service → اتصال به ریپوی GitHub.
۲. تنظیمات: Build Command = `echo ready` · Start Command = `node server/main.mjs` · Plan = Free · Region = Frankfurt.
۳. هر `git push` روی شاخهٔ `main` دیپلوی می‌سازد؛ اگر نساخت، از داشبورد یا API:
   `POST https://api.render.com/v1/services/{serviceId}/deploys` با هدر `Authorization: Bearer <کلید>`.
۴. سرویس فعلی: `yassaei-shop` (شناسهٔ srv-dah53bqjnfac738baj60). سرویس قدیمی `yassaei-legacy` فقط ریدایرکت ۳۰۱ است.
۵. سرویس رایگان بعد از بی‌کاری می‌خوابد؛ باز‌کردن لینک سایت خودش بیدارش می‌کند (۳۰–۶۰ ثانیه) و UptimeRobot با پینگ هر چند دقیقه بیدار نگهش می‌دارد. روی لوکال می‌توانی `node tools/keepalive.mjs` را اجرا کنی.

### ب) اگر Render نبود (VPS خودت/ایران)
```bash
# روی سرور لینوکس:
git clone <ریپوی خودت> && cd yassaei-electronics
nohup node server/main.mjs > app.log 2>&1 &     # یا systemd برای پایداری
```
- برای دسترسی از ایران بدون VPN: یک دامنهٔ `.ir` بخر و روی یک CDN/پراکسی داخلی (مثل ابر آروان/Cloudflare با نواد ایرانی) یا مستقیم روی IP سرور ایرانی باند کن.
- پورت ۸۰/۴۴۳ با nginx به عنوان reverse proxy جلوی Node بگذار و TLS را nginx بدهد.

### ج) تغییر کد و دیپلوی دوباره (چرخهٔ کامل)
```bash
# ویرایش فایل‌ها → تست لوکال → سپس:
git add -A && git commit -m "توضیح تغییر" && git push origin main
# Render خودش می‌سازد؛ اگر نساخت، دیپلوی دستی بزن (بند الف.۳)
```

---

## ۵) پنل مدیریت — فهرست کامل بخش‌ها ( شامل بخش‌های «مخفی»)

ورود: `#/admin` — هر بخش با مجوز جداگانه (ماتریس ~۲۰ مجوز):
- **داشبورد**: شاخص‌ها، نمودار بازدید، پرفروش‌ها، موجودی کم، آخرین رویدادها.
- **کارت «پدافند و صف ورود»** (زنده): درخواست فعال، rps، در صف، مسدودهای خودکار، پرمراجع‌ترین IPها، **خطاهای زندهٔ کلاینت** + فرم تنظیم آستانه‌ها.
- **کنسول سامانه**: ریستارت سرور، ریست بخش‌ها (ممیزی/سبدها/بازدیدها).
- کالاها، دسته‌ها، برندها (با جست‌وجوی تصویر خودکار و بارکد)، سفارش‌ها، کاربران و مجوزها، **مسدودسازی (بن) با مدت انقضا**، بازدیدکنندگان، نظرات، تیکت‌ها و چت پشتیبانی، بازخوردها، قرعه‌کشـی، کوپن‌ها، تبلیغات، اعلان‌ها و پخش همگانی (سایت/تلگرام/ایمیل/پیامک)، ربات تلگرام، **چت داخلی تیم**، **استخدام**، **چیدمان سایت**، پشتیبان‌گیری و بازیابی، رویدادها (ممیزی)، تنظیمات فروشگاه/تم/امکانات/پوسته/**ابزارهای فروش**.
- **خواب/بیداری فروشگاه** و زمان‌بندی بیداری خودکار.

## ۵٫۹) قابلیت‌های نسخهٔ رسمی v1.0.0 — هفت ستون (به زبان ساده)

### ۱) سقف خرید هوشمند (غلتان) — محافظ حساب بانکی فروشگاه
«سقف خرید غلتان» یعنی مجموع **خریدهای موفق** هر کاربر در ۴۸ ساعت گذشته شمرده می‌شود و از
سقف (پیش‌فرض ۱۰۰ میلیون تومان) کم می‌شود؛ اگر خریدار به سقف نزدیک شد، پیام محترمانهٔ
«تا X تومان می‌توانید سفارش بدهید» می‌بیند. نکات مهم:
- فقط سفارش **پرداخت‌شده** شمرده می‌شود؛ لغوشده/پرداخت‌نشده/مرجوع‌شده هرگز.
- محرومیت دائم وجود ندارد: بازه غلتان است، یعنی با گذشت زمان سهمیه خودبه‌خود برمی‌گردد.
- همه‌چیز از پنل: **تنظیمات ← سفارش و پرداخت** → کلید فعال/غیرفعال، مبلغ سقف، طول بازه (ساعت).
- یک محافظ دوم هم هست: «سقف روزانه» بر پایهٔ اثرانگشت دستگاه و کارت/شبای پرداخت، جلوی سیلاب
  سفارش‌های رباتیک با دستگاه/کارت یکسان را می‌گیرد.
- تست تخصصی: `npm run verify` بخش `order-ceiling` را می‌زند.

### ۲) واتساپ — دکمهٔ مشاوره و ارسال فیش
- در صفحهٔ هر کالا دکمهٔ **«مشاورهٔ فنی در واتساپ»** هست؛ مشتری با یک کلیک گفت‌وگو را با فروشگاه
  باز می‌کند. بعد از پرداخت هم دکمهٔ **«ارسال فیش در واتساپ»** نمایش داده می‌شود.
- شماره واتساپ را در **تنظیمات ← ابزارهای فروش** (`شمارهٔ واتساپ`) بگذار؛ اگر خالی باشد
  ترتیب جایگزین‌ها: واتساپ فروشگاه ← شبکه‌های اجتماعی ← موبایل دوم ← تلفن فروشگاه.
  هر فرمتی (`0912…`، `+98…`، `0098…`) خودکار به لینک درست `wa.me` تبدیل می‌شود.
- با کلید `whatsappConsultEnabled` می‌توانی دکمه را کامل خاموش کنی؛ بلافاصله از سایت مشتری حذف می‌شود.

### ۳) پیش‌فاکتور رسمی (Proforma)
- در **سبد خرید** دکمهٔ «دریافت پیش‌فاکتور رسمی» هست: سبد فعلی را به‌صورت برگهٔ رسمی با
  سربرگ فروشگاه، جدول کالاها، مبلغ، تاریخ شمسی و کد رهگیری یکتا می‌سازد و پرینت/PDF می‌دهد.
- فرمت کد: `PF-۱۴۰۵۰۶۲۸-XXXXX` (تاریخ شمسیِ روز + دنبالهٔ یکتا) — برای بایگانی و استعلام تلفنی عالی است.
- فعال/غیرفعال از **تنظیمات ← ابزارهای فروش** (`proformaInvoiceEnabled`). در کنارش «اطلاعات کارت‌به‌کارت»
  (نام بانک/شماره کارت/شبا/صاحب حساب) هم از همان‌جا پر می‌شود و در صفحهٔ پرداخت و فاکتور چاپ می‌شود.

### ۴) بخش استخدام (Careers)
- صفحهٔ عمومی `#/careers` برای مشتری‌ها: فهرست فرصت‌های شغلی باز + فرم درخواست
  (نام، موبایل، سن، تحصیلات، سابقه، آپلود رزومه اختیاری).
- پنل مدیر ← **استخدام**: ثبت/ویرایش/خاموش‌کردن فرصت‌ها و دیدن درخواست‌ها با وضعیت
  (در انتظار/تماس/… ) و یادداشت؛ هر درخواست جدید فوراً اعلان داخل پنل برای همهٔ مدیرها می‌فرستد.
- ضدسوءاستفاده: هر شماره موبایل برای هر شغل فقط یک درخواست می‌تواند بفرستد + محدودساز نرخ روزانه.
- اگر خواستی بخش را موقتاً ببندی: **تنظیمات ← امکانات** → کلید `careers` (دکمهٔ منو هم جدا قابل پنهان‌کردن است).

### ۵) چت داخلی تیم (فقط مدیرها و کارمندان)
- منوی پنل ← **چت تیم**: اتاق گفت‌وگوی خصوصی کادر فروشگاه — پیام متنی + پیوست (عکس/PDF)،
  منشن `@نام` با تکمیل خودکار، و ارجاع کلیک‌شدنی به `#سفارش` `#محصول` `#تیکت` `#کاربر`
  (مثلاً «این #order:123 را ببین» → لینک مستقیم همان سفارش).
- پیام‌ها زنده (بدون رفرش) به بقیهٔ تیم می‌رسند؛ فقط ۲۰۰ پیام اخیر نمایش و ۱۰۰۰ پیام آخر نگه داشته
  می‌شود — هرگز سنگین نمی‌شود.
- کاربر عادی هرگز به این اتاق دسترسی ندارد (۴۰۳)؛ کلید خاموش/روشن: **تنظیمات ← امکانات** → `adminChat`.

### ۶) مدیر چیدمان ادمین (Layout Manager)
- مسیر **پنل ← چیدمان سایت** (`#/admin/layout`): هر بلوک صفحهٔ اصلی (بنر هیرو، نوار برندها،
  شگفت‌انگیز، پرفروش‌ها، «چرا ما»، آمار زنده، بنر پلاس، «قطعه‌ای پیدا نکردی؟»، خرید دست‌دوم،
  نقشهٔ فروشگاه و…) را با **کشیدن‌ورهاکردن** جابه‌جا کن یا با یک کلیک **مخفی/ظاهر** کن.
- تب کناری «**مدیریت منوهای پنل مدیریت**»: نمایش/مخفی، تغییر نام و آیکون و ترتیبِ منوی پنل،
  منوی کاربری و نوار موبایل — بدون دست‌زدن به کد.
- ذخیره در `settings.layout` است و مثل بقیهٔ تنظیمات بین دیپلوی‌ها پایدار می‌ماند.

### ۷) ابزار نجات اضطراری (اگر خودت بن شدی)
- فاجعهٔ فرضی: پنل، بن خودکار را روی IP خودت فعال می‌کند و دیگر نمی‌توانی وارد شوی.
- راه‌حل از ترمینالِ سرور (نه از مرورگر): `node tools/unban-owner.mjs admin`
  (شماره موبایل هم کار می‌کند). این ابزار:
  • تمام بن‌های منسوب به آن حساب (نام‌کاربری، موبایل، ایمیل، IP‌های نشست‌ها، توکن بازدید) را پاک می‌کند
  • نقش و وضعیت حساب را به `owner / active` برمی‌گرداند
  • یک ردیف ممیزی ثبت می‌کند
  • **هیچ سفارش، موجودی یا داده‌ای حذف نمی‌شود** — فقط قفل‌های امنیتی باز می‌شوند.



---

## ۵٫۵) ربات تلگرام (yassaei_shop_bot) — وضعیت فعلی
توکن ست شده و بات **فعال و زنده** است. کارها: /start (عضویت در اعلان‌ها + خوش‌آمد)، /help، /status کدسفارش،
/products عبارت، /contact؛ هر پیام دیگر → صندوق ورودی پنل مدیر (بخش سامانه → ربات تلگرام) + پاسخ خودکار «ثبت شد».
پخش همگانی تلگرام از تب تبلیغات/پخش با کانال «telegram» انجام می‌شود.
اگر روزی توکن لو رفت: در BotFather دستور /revoke بزن و توکن جدید را در پنل بگذار.
نکتهٔ اجرای لوکالی: هم‌زمان با نسخهٔ زنده، لوکال را با `BM_TG=off` اجرا کن (پاسخ دوبله جلوگیری می‌شود).

## ۶) امنیت — چه چیزهایی جلوی دزد/هکر را گرفته؟

- رمز عبور: هش **scrypt** با salt اختصاصی + مقایسهٔ زمان‌ثابت (متن رمز هیچ‌جا ذخیره نمی‌شود).
- نشست: کوکی **HttpOnly + SameSite** با توکن ۲۵۶ بیتی؛ CSRF توکن دوثرفه روی همهٔ نوشتن‌ها.
- ورود دومرحله‌ای (TOTP/ایمیل/پیامک) + **قفل موقت پس از تلاش‌های ناموفق** + کپچای خودمیزبان.
- هدرهای امنیتی: CSP سخت‌گیرانه، HSTS، X-Frame-Options DENY، nosniff، COOP/COORP.
- محدودساز نرخ چندلایه + **مسدودسازی خودکار سیلاب** + اتاق انتظار ضد-DDoS.
- اعتبارسنجی همهٔ ورودی‌ها در سرور (کلاس `V`)؛ آپلود فقط تصویر با بررسی نوع/ابعاد.
- فایل دیتابیس بیرون از پوشهٔ عمومی است؛ هیچ مسیر استاتیکی به `data/` نمی‌رسد؛ path traversal مسدود.
- ممیزی (audit log) برای همهٔ عملیات حساس + گزارش خودکار خطاهای کلاینت به پنل.
- پشتیبان خودکار چندلایه + بازیابی خودکار هنگام خرابی فایل.
- **محافظت در برابر Inspect و برداشت کد** (`settings.security.antiInspectEnabled`، پیش‌فرض روشن):
  کلیک راست روی عناصر صفحه بسته است، میان‌برهای `F12` / `Ctrl+Shift+I/J/C` / `Cmd+Opt+I/J/C` / `Ctrl+U`
  مسدود می‌شود، هشدار امنیتی «فروشگاه یاسایی» در کنسول مرورگر چاپ می‌شود و تصاویر کالا `draggable="false"`
  هستند. داخل فیلدهای ورودی (input/textarea/select) همه‌چیز **آزاد** است؛ جست‌وجو، فرم‌ها، چت و لمس موبایل
  هیچ تغییری نمی‌کنند. کلید خاموش‌کردن: پنل مدیر → تنظیمات → تب **امنیت و محافظت از کد**
  (`#/admin/settings/security`). پارامترهای داخلی صف/محدودسازی نرخ هرگز به مرورگر فرستاده نمی‌شوند.

**کاری که خودت باید بکنی**: کلیدهای لو رفته را باطل کن (بند ۲)؛ رمز admin را عوض کن؛ توکن‌های تلگرام/SMTP/پیامک را فقط در پنل (نه در کد) بگذار.

---

## ۷) «خودترمیمی» سایت چه چیزی هست و چه چیزی نیست؟

**هست:**
۱. خرابی `db.json` → خودکار از `db.backup.json` و سپس تازه‌ترین اسنپ‌شات `data/backups/` برمی‌گردد.
۲. مجموعه‌های مفقود دیتابیس (مثلاً بر اثر ویرایش دستی) در راه‌اندازی و هر ۶۰ ثانیه بازسازی می‌شوند.
۳. خطای ناگیره در سرور → لاگ + ادامهٔ کار (سایت نمی‌افتد)؛ Render هم در کرش کامل، خودکار restart می‌کند.
۴. خطای جاوااسکریپت در مرورگر کاربر → خودکار به پنل مدیر گزارش می‌شود (کارت پدافند) تا سریع رفع شود.
۵. خطای یک بخش صفحه → فقط همان بخش پیام خطا می‌گیرد، بقیهٔ سایت کار می‌کند (error boundary).
۶. قطعی اینترنت کاربر → سرویس‌ورکر پوستهٔ آفلاین و دادهٔ کش‌شده نشان می‌دهد.

**نیست (و هیچ‌کس ندارد):** سایت نمی‌تواند کد خودش را بازنویسی کند. «رفع باگ توسط خود سایت» به‌معنی واقعی ممکن نیست؛
چیزی که نزدیک‌ترین حالت است همان ۶ مورد بالاست + گزارش خودکار خطا تا انسان/هوش مصنوعی سریع رفع کند.

---

## ۸) ایده‌هایی که از سایت‌های بزرگ گرفته شده (دیجی‌کالا/آمازون و…)

نوار پیشرفت «ارسال رایگان»، کالاهای مشابه در صفحه محصول، نوار خرید چسبان پایین موبایل، نشان موجودی کم («فقط ۲ عدد!»)،
«اخیراً دیده‌اید»، امتیاز و توزیع نظرات، جست‌وجوی fuzzy با «منظورتان این بود؟»، لیست علاقه‌مندی، مقایسهٔ کالا،
سبد مهمان که با ورود به حساب می‌چسبد، صفحهٔ صف ورود مثل فروشگاه‌های بزرگ هنگام حراج، ولکام‌اسکرین برندینگ‌شده.

---

## ۹) نگهداری روزمره (چک‌لیست هفتگی)

- [ ] داشبورد مدیر: سفارش‌های در انتظار، تیکت‌ها، نظرات در انتظار تأیید.
- [ ] کارت پدافند: «خطاهای زندهٔ کلاینت» خالی باشد؛ اگر خطایی دیدی یعنی باگی هست که باید رفع شود.
- [ ] پشتیبان‌ها: تب پشتیبان‌گیری → یک اسنپ‌شات دستی قبل از تغییرات بزرگ.
- [ ] موجودی کم و کالاهای ناموجود → بنر «اطلاع به موجودی» خودکار فعال است.
- [ ] UptimeRobot سبز باشد؛ اگر سایت خوابید، لینک را باز کن و تا بیدار شدن سرور (۳۰–۶۰ ثانیه) صبر کن.

---

## ۱۰) اگر خواستی سایت دیگری بسازی (مثلاً فروشگاه الکتریکی)

بله، کاملاً شدنی است و دو راه داری:
۱) **همین کد را کپی کن** و فقط «هستهٔ محتوا» را عوض کن: نام/لوگو/رنگ در تنظیمات مدیر، دسته‌ها و کالاها از پنل،
   متن صفحه‌ها از «محتوای صفحه‌ها». ساختار فروش یکسان است.
۲) **از صفر با هوش مصنوعی**: فایل `PROMPT-SAKHT-SITE.md` کنار همین راهنما هست؛ آن را به هر هوش مصنوعی بده
   و نوع جنس را عوض کن (مثلاً «لوازم الکتریکی و صنعتی»). آن پرامپت تمام درس‌های این پروژه را خلاصه کرده است.

---

## ۱۱) واژه‌نامهٔ کوتاه (برای یادگیری)

- **SPA**: کل سایت یک صفحه است و بخش‌ها با جاوااسکریپت عوض می‌شوند (سریع‌تر، بدون رفرش).
- **SW (سرویس‌ورکر)**: اسکریپتی که مثل واسطه بین مرورگر و شبکه می‌نشیند؛ کش و آفلاین را ممکن می‌کند.
- **CSRF / CSP / HSTS**: سه لایهٔ امنیتی مرورگری که به‌ترتیب جعل درخواست، تزریق کد و downgrade حمله را می‌بندند.
- **Rate Limit / Waiting Room**: شیرِ فلکهٔ ترافیک؛ وقتی شلوغ شد به‌جای سقوط، نوبت می‌دهد.
- **Atomic Write**: فایل دیتابیس اول کامل در فایل موقت نوشته و بعد جایگزین می‌شود → هیچ‌وقت نیمه‌خراب نمی‌ماند.
- **scrypt**: الگوریتم هش رمز که حتی اگر دیتابیس لو برود، رمزها قابل استخراج نباشند.

---

## ۱۲) حل مشکل‌های رایج

| مشکل | راه‌حل |
|---|---|
| سایت باز نمی‌شود / «در حال بیدارشدن» | لینک را باز کن و ۳۰–۶۰ ثانیه صبر کن (اسپین «بیدار شدن» یعنی در حال بالا آمدن)؛ مانیتور UptimeRobot را چک کن |
| خودم از پنل بیرون انداختم/بن شدم | `node tools/unban-owner.mjs <موبایل یا نام‌کاربری>` — فقط قفل‌های من پاک می‌شود، داده نمی‌رود |
| تغییرات من دیده نمی‌شود | کش سرویس‌ورکر: یک بار رفرش سخت (Ctrl+Shift+R)؛ نسخهٔ sw با هر انتشار عوض می‌شود |
| پنل مدیر باز نمی‌شود | رمز admin را عوض کرده‌ای؟ از همان مرورگری که نشست داری وارد شو؛ کوکی‌ها را پاک نکن |
| دیتابیس را دستی ویرایش کردم و سایت بالا نمی‌آید | سرور خودکار از backups برمی‌گردد؛ اگر نشد: آخرین فایل سالم را روی db.json کپی کن |
| می‌خواهم به تم قبلی برگردم | پنل مدیر → پوسته و چیدمان → «پوستهٔ ظاهری» = کلاسیک |

## ۱۳) دور بیست‌وسوم — چیزهایی که اضافه/درست شد (bm-v31)

### ۱۳٫۱) ربات تلگرام حالا «گفت‌وگوی کامل» دارد
- حالت اتصال بات عوض شد به **وب‌هوک**: تلگرام پیام‌ها را مستقیم به آدرس سایت push می‌کند (مسیر امن با کلید مخفی). قبلاً long-polling بود و هر نمونهٔ سرگردان سرور می‌توانست با خطای 409 کل بات را قفل کند — دلیل همان «هیچ جوابی نمی‌آید»های اولیه. اگر روزی وب‌هوک شکست بخورد، خودکار به polling برمی‌گردد و در پنل حالت اتصال نمایش داده می‌شود.
- باگ اصلی پیدا و حل شد: پاسخ /status و /products به‌شکل آرایه به تلگرام فرستاده می‌شد و تلگرام رد می‌کرد؛ کاربر هیچ پیامی نمی‌گرفت. حالا همهٔ پاسخ‌ها متن سادهٔ فارسی‌اند.
- /status بدون کد → راهنما می‌دهد («کد را مثل /status BM-123456 بفرست»)؛ کد اشتباه → می‌گوید از کجا کد درست را کپی کند.
- /products بدون عبارت → ۵ کالای نمونه + راهنمای جستجو؛ عبارت بی‌نتیجه → پیشنهاد عبارت کوتاه‌تر.
- دستور ناشناخته (مثلاً /stst) → به‌جای سکوت، فهرست دستورات را می‌فرستد.
- وضعیت سفارش فارسی و با توضیح مرحلهٔ بعد است (در انتظار پرداخت/آماده‌سازی/ارسال/تحویل/لغو/مرجوع).
- در پنل: سامانه → ربات تلگرام، خط «وضعیت اتصال ربات» اضافه شد (حالت وب‌هوک/پولینگ + آخرین خطا). اگر روزی بات جواب نداد، اول همین خط را ببین.

### ۱۳٫۲) کارت ربات در «تماس با ما» + پانویس
- صفحهٔ تماس با ما حالا بالای ستون کنار، کارت «ربات پشتیبانی تلگرام» با دکمهٔ «باز کردن ربات در تلگرام» دارد.
- آیکن تلگرام پانویس و دکمهٔ تلگرامِ فهرست شبکه‌های اجتماعی هم به t.me/yassaei_shop_bot وصل شدند.
- لینک از تنظیمات پنل (فروشگاه → شبکه‌های اجتماعی → تلگرام) قابل تغییر است؛ اگر خالی شود کارت خودبه‌خود پنهان می‌شود.

### ۱۳٫۳) ذره‌بین روی تصویر کالا (دسکتاپ)
- با بردن موس روی تصویر صفحهٔ کالا، یک ذره‌بین دایره‌ای ~۲٫۶ برابر بزرگ‌نمایی می‌کند (لبه‌ها هوشمند مهار می‌شوند).
- روی موبایل/لمسی غیرفعال است؛ آنجا دوضربه/کلیک = لایت‌باکس تمام‌صفحه مثل قبل.

### ۱۳٫۴) بستهٔ پست اینستاگرام (پنل → کالاها)
- کنار هر کالا یک آیکن دوربین اضافه شد: «بستهٔ پست اینستاگرام».
- مودال باز می‌شود با: تصویر کالا + دکمهٔ دانلود، متن پست آماده (نام، قیمت، تخفیف، موجودی، ضمانت‌ها، لینک سفارش، تلفن) قابل ویرایش، و هشتگ‌های آماده.
- سه دکمهٔ کپی: متن / هشتگ‌ها / متن+هشتگ. بعد فقط در اینستاگرام Paste کن.

### ۱۳٫۵) نگهبان تازگی نسخه (درمان کش قدیمی موبایل)
- اگر مرورگر/سرویس‌ورکرِ کاربر نسخهٔ کهنگرفته نگه داشته باشد، سایت خودش مقایسه می‌کند و تاست «نسخهٔ جدید سایت آماده است» با دکمهٔ تازه‌سازی نشان می‌دهد.
- ثبت سرویس‌ورکر حالا با updateViaCache:'none' است + هر ساعت و هر بار برگشت به تب، به‌روزرسانی بررسی می‌شود.

### ۱۳٫۶) باگ‌های نمایشی که درست شد
- جدول ارقام فارسیِ fmtTel یک رقم کم داشت («۲» جا نیفتاده بود): شماره‌ها رقم‌به‌رقم جابه‌جا نمایش می‌یافتند و رقم ۹ می‌شد undefined! حالا همهٔ شماره‌های تلفن/موبایل/واتساپ در کل سایت (فروشگاه + پنل) با جدول درست و بدون جداکنندهٔ هزارگان نمایش می‌یابند.
- جست‌وجوی صوتی: دیگر دو تاست «در حال گوش دادن» و خطا هم‌زمان نمی‌مانند؛ تاست گوش‌دادن به‌محض خطا/پایان بسته می‌شود.

## ۱۴) دور بیست‌وچهارم — شکار باگ مجدد (bm-v32)
- **escape ورودی کاربر در پاسخ ربات:** پاسخ‌ها با قالب HTML تلگرام فرستاده می‌شوند؛ اگر کاربر در /status یا /products علامت‌های < یا & می‌فرستاد، تلگرام send را رد می‌کرد و کاربر دوباره «بی‌پاسخی» می‌دید. حالا تمام fragmentهای ساخته‌شده از ورودی کاربر escape می‌شوند.
- **اعتبارسنجی لینک‌های شبکه‌های اجتماعی:** لینک‌های تلگرام/اینستاگرام/ایتا که در پنل ثبت می‌شوند حالا فقط با http/https پذیرفته می‌شوند (در سرور هنگام ذخیره + در کلاینت هنگام رندر). این یعنی پروتکل‌های خطرناک مثل javascript: هرگز داخل href نمی‌رسند.
- **حذف نشت جزئی اطلاعات:** نام سرویس Render از پاسخ عمومی /api/system/status حذف شد (فقط در پنل ادمین دیده می‌شود).
- **بازیابی فایل‌ها:** چند فایل کلاینت که در اثر برگشت نسخهٔ ورک‌اسپیس قدیمی شده بودند (نمایش شماره با fmtTel، ذره‌بین، بستهٔ پست، کارت ربات، خط سلامت ربات) از مخزن git بازیابی و با diff کامل کل درخت فایل راستی‌آزمایی شدند.

## ۱۵) راند ۲۵ — رفع باگ چینش داشبورد ادمین + بازسازی سرویس ریدایرکت (۱۴۰۴/۰۶/۱۹)
- **باگ:** در عرض‌های ≥۹۴۰px کارت «پدافند/صف» داشبورد (و بخش‌های پهن صفحهٔ کاربران) داخل ستون ۲۵۰pxیِ `.acc-grid` له می‌شد و محتوا روی هم می‌افتاد (اسکرین‌شات کاربر).
- **رفع:** کلاس جدید `.acc-grid-eq` (دو ستون مساوی فقط از ۱۱۰۰px به بالا، تک‌ستون زیر آن) روی داشبورد و کاربران + `min-width:0` برای بچه‌های گرید + تغییر `.form-grid .span-2` از `span 2` به `1/-1` تا دکمهٔ ذخیره در فرم تک‌ستونه بیرون نزند. نسخه bm-v33.
- **سرویس ریدایرکت:** سرویس قدیمی legacy خراب شده بود (no-server). حذف و سرویس جدید با نام **bander-mobile** ساخته شد (همان slug قدیمی تا ۳۰۱ کار کند): `srv-dahbrep42hec73flnvi0`. شرط skip بات تلگرام برای این نام هم محکم شد (`server/lib/telegram.mjs`).
- **نکتهٔ مهم دیپلوی:** db.json ریپو «seed» نسخهٔ زنده است؛ تنظیمات تلگرام (توکن/خوش‌آمد) حالا داخل seed هست تا با هر دیپلوی بات نپرد.


## آخرین تغییرات و وضعیت پایداری نهایی (نسخه تضمین‌شده - ۲۴ شهریور ۱۴۰۵)
این نسخه یک نسخه کاملاً پایدار، باگ‌گیری شده و نهایی است که تمامی امکانات زیر در آن تست و تأیید شده‌اند:
۱. **فاکتورساز لحظه‌ای و چاپ (Print Invoice):** کدها جوری بازنویسی شدند که هنگام فشردن دکمه چاپ، فاکتور از لایه‌های تودرتوی ادمین جدا شده و مستقلاً چاپ شود تا مشکل سفیدی صفحات و تنظیم نبودن ابعاد کاغذ به طور کامل برطرف گردد.
۲. **امنیت حافظه و مقابله با Database Bloat:** تمام آرایه‌های در حال رشد سیستم (نظیر `supportMessages`, `carts`, `visitors`, `audit`, `reviews` و لیست تراکنش‌های کیف پول) به طور دقیق محدود شده‌اند (سقف ۵۰ تا ۵۰۰ رکورد بسته به کاربرد) تا سرور هرگز دچار کمبود حافظه (DoS) نشود.
۳. **رفع پاپ‌آپ‌های مزاحم بروزرسانی:** یکپارچه‌سازی متغیر `BUILD` بین فرانت‌اند (`state.mjs`) و بک‌اند (`util.mjs`) به منظور جلوگیری از نمایش خطای "نسخه جدید سایت آماده است" در مواقعی که آپدیتی وجود ندارد.
۴. **ایزوله‌سازی پروژه‌ها:** پروژه سیب سبز (موبایل) و پروژه یاسایی (الکترونیک) به صورت ۱۰۰٪ مجزا بدون هیچگونه تداخل کدی مدیریت و مستندسازی شده‌اند.
۵. **فایل‌های آفلاین:** نسخه‌های `offline.html` با کدهای جدید همگام‌سازی شده و از طریق `standalone-build.mjs` بازسازی کامل شده‌اند.

## ۱۷) انتشار رسمی `v1.0.0` — ۱۴۰۵/۰۶/۲۹ (نسخهٔ گیت‌هاب)
این نسخه اولین **Release رسمی** مخزن `demonsp/yassaei-electronics` است:
- **تگ:** `v1.0.0` روی `main` · **ضمیمه:** `yassaei-offline.html` (نسخهٔ تک‌فایلی آفلاین، بازسازی‌شده با
  `node tools/standalone-build.mjs` — بدون هیچ راز/توکن/دادهٔ مشتری؛ بذر نمایشی با رمز `Demo@1404`).
- **سبزبودن کامل:** `npm run build && npm run verify` — هر ۲۳ سوئیت تست + جاروب رازها بدون خطا.
- **مستندات هم‌نسخه:** `README.md` (ویژگی‌ها) · `STRUCTURE.md` (نقشهٔ کد + جدول «۷ ستون») ·
  `RAHNAMA-KAMEL.md` (همین راهنما) · `PROMPT-SAKHT-SITE.md` (پرامپت بازتولید) — هر چهار فایل
  ویژگی‌های «سقف هوشمند، واتساپ، پیش‌فاکتور، استخدام، چت تیم، مدیر چیدمان، نجات اضطراری» را پوشش می‌دهند.
- **متادیتای مخزن:** توضیح فارسی/انگلیسی + تاپیک‌های `electronics · electrical · ecommerce · pwa · tehran · iran`.
- **از این به بعد برای هر نسخهٔ تازه:** `npm run bump` (تگ نسخه) → تست → `git push` → دیپلوی خودکار Render
  → `gh release create vX.Y.Z yassaei-offline.html --title … --notes …`

## ۱۶) دورهای ۲۶ تا ۲۸ — رفع باگ ورود، امنیت خروجی عمومی، بررسی یک‌دستوری (ys-v125…ys-v127)

### ys-v125 — «ورود کار نمی‌کند» برای نام کاربری بدون رقم (باگ واقعی)
- **علامت:** تست‌ها وسط اجرا می‌شکستند و کاربر نمونهٔ مریم اصلاً نمی‌توانست وارد شود.
- **ریشه:** در تطبیق هویت هنگام ورود، شرط `u.phone === identifier.replace(/\D/g,'')` برای هر شناسهٔ
  بدون رقم به رشتهٔ خالی تبدیل می‌شد؛ یعنی «maryam» با اولین کاربرِ بی‌موبایل (حساب مدیر) یکی گرفته
  می‌شد. اگر رمزها یکی بودند، احتمال ورود به حساب اشتباه هم داشت.
- **رفع:** تطبیق دقیق (نام کاربری/ایمیل، سپس موبایل و فقط وقتی شناسه واقعاً ≥۱۰ رقم باشد؛ +98 هم پذیرفته می‌شود).
  تست رگرسیون اضافه شد.
- **تست‌ها:** حساب‌های نمونه و حساب کارمند از مسیر رسمی API ساخته می‌شوند (`tests/lib/demo.mjs`)؛
  ادعاهای عددیِ شکننده (تعداد دسته/برند) به بررسی ساختاری و رفت‌وبرگشت داده تبدیل شدند.

### ys-v126 — هیچ رمز/توکن/دادهٔ مشتری در فایل عمومی آفلاین
- **مشکل:** `yassaei-offline.html` (که پخش می‌شود) کل دیتابیس را در خود داشت: هش رمز، راز دومرحله‌ای،
  نشست‌ها، بازدیدکنندگان با IP، سفارش/تیکت مشتریان و رمز واقعی مدیر + توکن زندهٔ ربات.
- **رفع:** `tools/offline-seed.mjs` بذر را پاک‌سازی می‌کند (رمز نمایشی `Demo@1404`، بدون هش/راز، تنظیمات
  حساس خالی، مجموعه‌های شخصی خالی). `tools/secret-scan.mjs` هم اضافه شد تا این باگ برنگردد.
- **توکن ربات:** از کد بیرون رفت؛ `BM_TG_TOKEN` (متغیر محیطی) اولویت دارد و پنل پشتوانه است.

### ys-v127 — یک دستور برای اطمینان کامل
- `npm run verify` → سرور آزمایشی روی پورت آزاد و پوشهٔ دادهٔ **جدا** (`BM_DATA_DIR`) بالا می‌آید،
  همهٔ سوئیت‌های تست (امروز ۲۳ سوئیت) + جاروب رازها اجرا و خلاصه چاپ می‌شود؛ `data/db.json` دست‌نخورده می‌ماند.
- `npm run bump` → تگ BUILD هم‌زمان در هر ۵ جا جلو می‌رود و باندل/نسخهٔ آفلاین بازسازی می‌شود.
- سازندهٔ کپچا در تست‌ها تک‌نسخه شد (یک باگ محاسبه در نسخهٔ تازهٔ آن پیدا و رفع شد).

**یادآور امنیتی که باید خودت انجام بدهی:** توکن ربات تلگرام در گیت منتشر شده بود؛ در BotFather دستور
/revoke بزن و توکن جدید را در Render (Environment → `BM_TG_TOKEN`) بگذار. رمز ادمین را هم اگر هنوز
`Yassaei@1404` است، عوض کن.

---

## ۱۸) راند ۳۳ — محافظت در برابر Inspect و برداشت کد + یکدست‌سازی تاریخ شمسی (شهریور ۱۴۰۵)

**تاریخ رسمی این راند: شهریور ۱۴۰۵ هجری شمسی (1405 SH).** این نسخه بدون هیچ رگرسیونی روی فروشگاه
منتشر شد؛ همهٔ امکانات قبلی (سبد، سفارش، فاکتور، تلگرام، استخدام، چت تیم، چیدمان، سقف خرید) دست‌نخورده‌اند.

### چه چیزی اضافه شد؟
- **کلید تازه در تنظیمات امنیتی:** `settings.security.antiInspectEnabled` با **پیش‌فرض روشن (`true`)**.
  مسیر پنل مدیر: **`#/admin/settings/security`** (تب «امنیت و محافظت از کد» در صفحهٔ تنظیمات) — همان
  صفحهٔ کامل امنیت که کلیدهای صف/محدودسازی نرخ را هم یک‌جا نشان می‌دهد. کارت امنیتی داشبورد هم یک
  دکمهٔ میان‌بر به همین صفحه گرفت.
- **کلیک راست:** روی عناصر عادی صفحه بسته است؛ داخل `input`، `textarea`، `select` و
  `contenteditable` کاملاً آزاد (چسباندن، غلط‌یاب، انتخاب متن، کپی)— تایپ کاربر هیچ محدودیتی ندارد.
- **میان‌برهای DevTools:** `F12`، `Ctrl+Shift+I`، `Ctrl+Shift+J`، `Ctrl+Shift+C`، `Ctrl+U`
  (نمایش سورس) و در مک `Cmd+Opt+I/J/C`. هیچ کلید سالمی بسته نمی‌شود: تایپ، `Ctrl+C/V/S`، ناوبری و
  فرم‌ها آزادند.
- **هشدار کنسول:** متن رسمی و ثابت فروشگاه با استایل قرمز و توپر:
  «هشدار امنیتی فروشگاه یاسایی: هرگونه تلاش برای دستکاری، بازرسی یا کپی‌برداری غیرمجاز ثبت و پیگرد قانونی دارد.»
- **ضدکشیدن تصاویر:** همهٔ تصاویر (از جمله تصاویری که بعداً با کاتالوگ/گالری/چت رندر می‌شوند)
  `draggable="false"` می‌گیرند؛ اما **کشیدن‌ورهاکردن پنل مدیریت** (چیدمان صفحه و منوها) چون
  `draggable="true"` است، آزاد مانده است.
- **خاموش‌کردن فوری:** با ذخیرهٔ کلید در پنل، همان لحظه (از طریق رویداد `settings`) محافظت برداشته
  می‌شود و نشانه‌های `draggable` هم پاک می‌شوند؛ روشن‌کردن دوباره هم بلافاصله اعمال می‌شود.

### چه چیزی عمداً **انجام نشد**
- هیچ تلاش برای «قفل‌کردن کد» یا پنهان‌کاری نیست؛ هر کد سمت مرورگر قابل مطالعه است. این لایه فقط یک
  **بازدارندهٔ حرفه‌ای** بر پایهٔ قوانین مرورگر است تا کپی‌برداری و دستکاری سهو‌انگیز سخت شود.
- هیچ مانعی برای دسترس‌پذیری (کیبورد، screen-reader، لمس موبایل) ساخته نشد.

### چطور مطمئن شویم؟ (تست خودکار)
- `node tests/anti-inspect.mjs` → ۳۹ بررسی: رفتار DOM واقعی (jsdom) + کلید امنیتی روی سرور زنده
  (پیش‌فرض، ذخیره/خواندن، دسترسی مهمان، باقی‌ماندن تنظیمات صف). این سوئیت در `npm run verify` هم اجرا می‌شود.
- میان‌بر کامل: `npm run build && npm run verify` → **۲۳ سوئیت + جاروب رازها، همه سبز**.
- اکنون `tests/render-admin.mjs` تب `settings/security` را هم رندر می‌کند و وجود کنترل‌های
  `antiInspectEnabled` و تنظیمات صف را بررسی می‌کند.

### یکدست‌سازی تاریخ‌ها
- تاریخ رسمی همهٔ اسناد و نسخه‌ها **شهریور ۱۴۰۵ هجری شمسی (1405 SH)** است؛ هیچ نشانی از سال‌های
  اشتباه تایپی (مثل «۱۳۰۵») در هیچ فایل مستنداتی وجود ندارد. تاریخ‌های تاریخیِ راندها (مثلاً ۱۴۰۴/۰۶/۱۹)
  به‌عمد دست‌نخورده مانده‌اند تا سابقهٔ واقعی پروژه حفظ شود، و در سرصفحهٔ همین سند تاریخ رسمی صریح آمده است.
- فایل `README-LOCAL.md` که از پروژهٔ قدیمی نام «تهران موبایل» و فایل `bander-mobile-offline.html` را
  داشت، به برند رسمی **«الکتریکی و قطعات الکترونیک یاسایی تهران»** و فایل `yassaei-offline.html`
  (کلید `ys_stand_db_v2`) اصلاح شد.
